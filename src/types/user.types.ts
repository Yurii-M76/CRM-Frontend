export type TMe = {
  id: string;
  name: string;
  email: string;
  roles: string[];
  iat: number;
  exp: number;
};

export type TUser = {
  id: string;
  name: string;
  email: string;
  roles: string;
};